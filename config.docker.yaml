proxy:
  http_addr: ":8124"
  native_addr: ":9001"

admin:
  addr: ":8125"

postgres:
  dsn: "postgres://chtenant:chtenant@postgres:5432/chtenant?sslmode=disable"

clusters:
  - id: default
    http_endpoint: "http://clickhouse:8123"
    native_endpoint: "clickhouse:9000"
    user: "default"
    password: ""
